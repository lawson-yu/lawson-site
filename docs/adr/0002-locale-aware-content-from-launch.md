# Store content by locale from launch

Launch Chinese-only with `zh-CN` as the default locale, while storing all content, URLs, metadata, and import documents with an explicit locale. This avoids a costly content migration before the planned English release, without adding a language switcher or translation obligation to the first version.
